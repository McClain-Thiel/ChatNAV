FROM python:3.11-slim

LABEL maintainer="McClain Thiel"
LABEL description="Neoantigen pipeline Module 5: Clonality estimation (PyClone-VI)"
LABEL version="0.1.0"

RUN pip install --no-cache-dir \
    pyclone-vi>=0.1.0 \
    pandas>=2.0 \
    numpy>=1.24 \
    h5py>=3.9

WORKDIR /work
ENTRYPOINT []
